{
  "openapi": "3.1.0",
  "paths": {
    "/hello": {
      "get": {
        "summary": "Returns a simple greeting message.\nUseful for testing basic GET requests and server health.",
        "operationId": "hello"
      }
    },
    "/hello/{name}": {
      "get": {
        "summary": "Returns a personalized greeting message using path parameters.",
        "operationId": "hello_user"
      }
    },
    "/echo": {
      "post": {
        "summary": "Echoes back the JSON payload provided by the user.\nValidates input using a Pydantic model.",
        "operationId": "echo"
      }
    }
  }
}